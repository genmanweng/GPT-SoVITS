version: '3.8'

services:
  gpt-sovits:
    image: gpt-sovits-container
    build:
      context: .
    container_name: gpt-sovits-container
    environment:
      # 启用半精度推理, 根据实际情况调整
      - is_half=False
      - is_share=False
    volumes:
      - ./output:/workspace/output
      - ./video_file:/workspace/video_file
      - ./logs:/workspace/logs
      - ./SoVITS_weights:/workspace/SoVITS_weights
      - ./reference:/workspace/reference
    working_dir: /workspace
    ports:
      # API 端口
      - "9880:9880"
      # 校对工具端口
      - "9871:9871"
      # 推理界面端口
      - "9872:9872"
      # UVR5界面端口
      - "9873:9873"
      # 主界面端口
      - "9874:9874"
    shm_size: 16G
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #       - driver: nvidia
    #         count: "all"
    #         capabilities: [gpu]
    stdin_open: true
    tty: true
    restart: unless-stopped
